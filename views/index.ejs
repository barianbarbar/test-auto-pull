<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Docker | CRUD With Node & Mongo</title>
</head>
<body>
    <% if (activePage === "home") { %>
        <%- include("pages/formAddTask") %> 
    <% } else if (activePage === "list") { %> 
        <%- include("pages/listAllTask") %> 
    <% } else if (activePage === "edit") { %> 
        <%- include("pages/formEditTask") %> 
    <% } %> 
</body>
</html>